export const cookieOptions = {
    name: "PRT-admin-program",
    sameSite: "strict" as const,
    secure: true
};

export const BUILD_cookieOptions = {
    name: "PRT-build-token",
    sameSite: "strict" as const,
    secure: true,
}